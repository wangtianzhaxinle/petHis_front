import request from '@/utils/request'

export function getAllEmployeeInfoList(data) {
  return request({
    url: '/employee/getAllEmployeeInfoList',
    method: 'get',
    params: {
      pageSize: data.pageSize,
      pageNum: data.pageNum
    }

  })
}
